/* globals define */

define('app', ['jquery', 'underscore', 'bootstrap'], function ($, _) {
    'use strict';

    var application = {
        init: function () { console.log('JS initialized!'); }
    };

    return application;
});